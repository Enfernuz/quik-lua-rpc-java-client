node {

    stage('Checkout') {
        checkout scm
    }

    stage('Build') {

        // TO-DO: add 'withMaven(...)' step
        // sh 'mvn clean compile test-compile'
    }

    stage('Run the unit tests') {

        // TO-DO: add 'withMaven(...)' step
        // sh 'mvn test'
    }
}